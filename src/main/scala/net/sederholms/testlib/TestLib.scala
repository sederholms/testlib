package net.sederholms.testlib

/** A library class with one method */
class TestLib {
  /** Returns the message that is provided as parameter
    *
    * @param Message for the method
    * @return Returns the same message that was provided as parameter
    */
  def test(message: String): String = message
}
